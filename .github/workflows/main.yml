name: 'Linting & test'

on: push

jobs:
  Linting:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 'Installs packages'
        run: npm install
      - name: 'Execute linter'
        run: npm run lint
    